namespace DaiQuery
{
    /// <summary>
    /// Collection of all keywords that mark the beginning of SQL clauses.
    /// </summary>
    internal enum eClauseKeyword
    {
        FROM,
        WHERE,
        SELECT,
        UPDATE,
        SET,
        DELETE
    }
}
