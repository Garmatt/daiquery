namespace DaiQuery
{
    /// <summary>
    /// Collection of all keywords that mark the beginning of SQL clauses.
    /// </summary>
    internal enum ClauseKeyword
    {
        From,
        Where,
        Select,
        Update,
        Set,
        Delete
    }
}
